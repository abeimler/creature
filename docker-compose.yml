version: '2'

services:
  build-gcc-5:
    build:
      content: ./docker/gcc-5
    container_name: build-gcc-5
    volumes:
      - .:/source
    environment:
      - BUILD_TYPE=Release
      - BUILD_PATH=/binary